---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: autostagers.autostager.autostager.com
spec:
  group: autostager.autostager.com
  names:
    kind: Autostager
    listKind: AutostagerList
    plural: autostagers
    singular: autostager
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Autostager is the Schema for the autostagers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AutostagerSpec defines the desired state of Autostager
            properties:
              containerPort:
                format: int32
                type: integer
              helmMode:
                description: Foo is an example field of Autostager. Edit autostager_types.go
                  to remove/update
                type: boolean
              image:
                type: string
              ingressHost:
                type: string
              namespace:
                type: string
              replicas:
                format: int32
                type: integer
            required:
            - containerPort
            - helmMode
            - image
            - ingressHost
            - namespace
            type: object
          status:
            description: AutostagerStatus defines the observed state of Autostager
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
